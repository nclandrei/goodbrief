---
interface Props {
  class?: string;
}

const { class: className = '' } = Astro.props;
---

<section class={`py-16 px-8 ${className}`}>
  <div class="max-w-2xl mx-auto text-center">
    <h2 class="font-serif text-4xl md:text-5xl text-gray-900 mb-6">
      Primește vești bune, nu breaking news
    </h2>
    <p class="text-lg text-gray-600 mb-12">
      În fiecare luni dimineața, un singur email cu cele mai faine vești din România. Scurt, calm, feel-good only.
    </p>
    
    <!-- TODO: Replace with EmailOctopus embed form -->
    <form 
      class="flex flex-col sm:flex-row gap-6 max-w-xl mx-auto mb-8"
      action="#" 
      method="POST"
    >
      <input
        type="email"
        name="email"
        placeholder="adresa@email.ro"
        required
        class="flex-1 px-2 py-3 bg-transparent border-b-2 border-gray-300 focus:border-coral-600 outline-none transition-colors text-center sm:text-left"
      />
      <button
        type="submit"
        class="px-8 py-3 bg-coral-600 text-white font-medium hover:bg-coral-700 transition-colors"
      >
        Vreau în listă
      </button>
    </form>
    
    <p class="text-sm text-gray-500">
      Prin înscriere ești de acord să primești newsletter-ul Good Brief. Te poți dezabona oricând. Vezi <a href="/privacy" class="underline hover:text-coral-600">Politica de confidențialitate</a>.
    </p>
  </div>
</section>
