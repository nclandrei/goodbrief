---
const navLinks = [
  { href: '/', label: 'Home' },
  { href: '/issues', label: 'Arhivă' },
  { href: '/about', label: 'Despre' },
];
---

<header>
  <nav class="max-w-5xl mx-auto px-4 py-6 flex items-center justify-between">
    <a href="/" class="text-2xl font-serif font-bold text-gray-800 hover:text-gray-900 transition-colors">
      Good Brief
    </a>
    
    <ul class="flex items-center gap-8">
      {navLinks.map((link) => (
        <li>
          <a 
            href={link.href} 
            class="text-gray-600 hover:text-coral-600 transition-colors text-xs font-medium uppercase tracking-wide"
          >
            {link.label}
          </a>
        </li>
      ))}
    </ul>
  </nav>
</header>
